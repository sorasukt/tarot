(() => {
  const $=id=>document.getElementById(id);
  addEventListener("DOMContentLoaded",()=>{$("supportForm").addEventListener("submit",submit);document.querySelectorAll("[data-amount]").forEach(button=>button.addEventListener("click",()=>{$("supportAmount").value=button.dataset.amount;$("supportAmount").focus()}));if(new URLSearchParams(location.search).has("canceled"))$("supportMessage").textContent="ยังไม่มีการเรียกเก็บเงิน คุณสามารถกลับมาสนับสนุนเมื่อพร้อม"});
  async function submit(event){event.preventDefault();const amount=Number($("supportAmount").value),button=$("supportButton"),message=$("supportMessage");if(!Number.isInteger(amount)||amount<50||amount>100000){message.textContent="กรุณาระบุจำนวนเงิน 50–100,000 บาท";return}if(!$("supportAccepted").checked){message.textContent="กรุณายอมรับข้อกำหนดก่อนดำเนินการ";return}window.TarotPortal.setButtonBusy(button,true,"กำลังเปิดหน้าชำระเงิน…");message.textContent="กำลังพาคุณไปเลือก PromptPay หรือบัตร";try{const response=await window.TarotPortal.api("/api/billing/checkout/support",{method:"POST",headers:{"Content-Type":"application/json","X-Tarot-Policy-Version":window.TarotPortal.policyVersion},body:JSON.stringify({amount,accepted:true,requestId:crypto.randomUUID()}),timeout:20000}),data=await response.json();if(!response.ok)throw window.TarotPortal.apiError(data,"เริ่มการสนับสนุนไม่สำเร็จ");if(!/^https:\/\/checkout\.stripe\.com\//.test(data.url||""))throw new Error("ลิงก์ชำระเงินไม่ถูกต้อง");location.assign(data.url)}catch(error){window.TarotPortal.renderError(message,error);window.TarotPortal.setButtonBusy(button,false)}}
})();
